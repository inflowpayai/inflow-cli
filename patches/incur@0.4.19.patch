diff --git a/dist/Cli.d.ts b/dist/Cli.d.ts
index 1d2d12a..36def74 100644
--- a/dist/Cli.d.ts
+++ b/dist/Cli.d.ts
@@ -211,9 +211,11 @@ export declare namespace create {
             error: (options: {
                 code: string;
                 cta?: CtaBlock | undefined;
+                details?: unknown;
                 exitCode?: number | undefined;
                 message: string;
                 retryable?: boolean | undefined;
+                title?: string | undefined;
             }) => never;
             /** The resolved output format (e.g. `'toon'`, `'json'`, `'jsonl'`). */
             format: Formatter.Format;
@@ -427,9 +429,11 @@ type CommandDefinition<args extends z.ZodObject<any> | undefined = undefined, en
         error: (options: {
             code: string;
             cta?: CtaBlock | undefined;
+            details?: unknown;
             exitCode?: number | undefined;
             message: string;
             retryable?: boolean | undefined;
+            title?: string | undefined;
         }) => never;
         /** The resolved output format (e.g. `'toon'`, `'json'`, `'jsonl'`). */
         format: Formatter.Format;
@@ -452,4 +456,4 @@ type CommandDefinition<args extends z.ZodObject<any> | undefined = undefined, en
         version: string | undefined;
     }): InferReturn<output> | Promise<InferReturn<output>> | AsyncGenerator<InferReturn<output>, unknown, unknown>;
 };
-//# sourceMappingURL=Cli.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=Cli.d.ts.map
diff --git a/dist/Cli.js b/dist/Cli.js
index 1824430..4f60ed8 100644
--- a/dist/Cli.js
+++ b/dist/Cli.js
@@ -1131,8 +1131,10 @@ async function serveImpl(name, commands, argv, options = {}) {
                         ok: false,
                         error: {
                             code: err.code,
+                            ...(err.details !== undefined ? { details: err.details } : undefined),
                             message: err.message,
                             ...(err.retryable !== undefined ? { retryable: err.retryable } : undefined),
+                            ...(err.title !== undefined ? { title: err.title } : undefined),
                         },
                         meta: {
                             command: path,
@@ -1262,11 +1264,13 @@ async function serveImpl(name, commands, argv, options = {}) {
             ok: false,
             error: {
                 code: result.error.code,
+                ...(result.error.details !== undefined ? { details: result.error.details } : undefined),
                 message: result.error.message,
                 ...(result.error.retryable !== undefined
                     ? { retryable: result.error.retryable }
                     : undefined),
                 ...(result.error.fieldErrors ? { fieldErrors: result.error.fieldErrors } : undefined),
+                ...(result.error.title !== undefined ? { title: result.error.title } : undefined),
             },
             meta: {
                 command: path,
@@ -1561,8 +1565,10 @@ async function executeCommand(path, command, rest, inputOptions, request, start,
             ok: false,
             error: {
                 code: err.code,
+                ...(err.details !== undefined ? { details: err.details } : undefined),
                 message: err.message,
                 ...(err.retryable !== undefined ? { retryable: err.retryable } : undefined),
+                ...(err.title !== undefined ? { title: err.title } : undefined),
             },
             meta: meta(formatCtaBlock(options.name ?? path, err.cta)),
         });
@@ -1624,11 +1630,13 @@ async function executeCommand(path, command, rest, inputOptions, request, start,
             ok: false,
             error: {
                 code: result.error.code,
+                ...(result.error.details !== undefined ? { details: result.error.details } : undefined),
                 message: result.error.message,
                 ...(result.error.retryable !== undefined
                     ? { retryable: result.error.retryable }
                     : undefined),
                 ...(result.error.fieldErrors ? { fieldErrors: result.error.fieldErrors } : undefined),
+                ...(result.error.title !== undefined ? { title: result.error.title } : undefined),
             },
             meta: {
                 command: path,
@@ -1802,12 +1810,15 @@ function extractBuiltinFlags(argv, options = {}) {
             format = 'json';
             formatExplicit = true;
         }
-        else if (token === '--format' && argv[i + 1]) {
-            if (!validFormats.has(argv[i + 1]))
+        else if (token === '--format') {
+            const value = argv[i + 1];
+            if (value === undefined)
+                throw new ParseError({ message: 'Missing value for flag: --format' });
+            if (!validFormats.has(value))
                 throw new ParseError({
-                    message: `Invalid format: "${argv[i + 1]}". Expected one of: ${[...validFormats].join(', ')}`,
+                    message: `Invalid format: "${value}". Expected one of: ${[...validFormats].join(', ')}`,
                 });
-            format = argv[i + 1];
+            format = value;
             formatExplicit = true;
             i++;
         }
@@ -2253,6 +2264,8 @@ const toRootAliases = new WeakMap();
 const sentinel = Symbol.for('incur.sentinel');
 /** @internal Formats an error for human-readable TTY output. */
 function formatHumanError(error) {
+    if (error.title !== undefined)
+        return `${error.title}\n\n${error.message}`;
     const prefix = error.code === 'UNKNOWN' || error.code === 'COMMAND_NOT_FOUND'
         ? 'Error'
         : `Error (${error.code})`;
@@ -2304,14 +2317,16 @@ async function handleStreaming(generator, ctx) {
                                 ok: false,
                                 error: {
                                     code: tagged.code,
+                                    ...(tagged.details !== undefined ? { details: tagged.details } : undefined),
                                     message: tagged.message,
                                     ...(tagged.retryable !== undefined
                                         ? { retryable: tagged.retryable }
                                         : undefined),
+                                    ...(tagged.title !== undefined ? { title: tagged.title } : undefined),
                                 },
                             }));
                         else
-                            ctx.writeln(formatHumanError({ code: tagged.code, message: tagged.message }));
+                            ctx.writeln(formatHumanError({ code: tagged.code, message: tagged.message, title: tagged.title }));
                         ctx.exit(tagged.exitCode ?? 1);
                         return;
                     }
@@ -2330,12 +2345,14 @@ async function handleStreaming(generator, ctx) {
                         ok: false,
                         error: {
                             code: err.code,
+                            ...(err.details !== undefined ? { details: err.details } : undefined),
                             message: err.message,
                             ...(err.retryable !== undefined ? { retryable: err.retryable } : undefined),
+                            ...(err.title !== undefined ? { title: err.title } : undefined),
                         },
                     }));
                 else
-                    ctx.writeln(formatHumanError({ code: err.code, message: err.message }));
+                    ctx.writeln(formatHumanError({ code: err.code, message: err.message, title: err.title }));
                 ctx.exit(err.exitCode ?? 1);
                 return;
             }
@@ -2392,8 +2409,10 @@ async function handleStreaming(generator, ctx) {
                             ok: false,
                             error: {
                                 code: tagged.code,
+                                ...(tagged.details !== undefined ? { details: tagged.details } : undefined),
                                 message: tagged.message,
                                 ...(tagged.retryable !== undefined ? { retryable: tagged.retryable } : undefined),
+                                ...(tagged.title !== undefined ? { title: tagged.title } : undefined),
                             },
                             meta: {
                                 command: ctx.path,
@@ -2412,8 +2431,10 @@ async function handleStreaming(generator, ctx) {
                     ok: false,
                     error: {
                         code: err.code,
+                        ...(err.details !== undefined ? { details: err.details } : undefined),
                         message: err.message,
                         ...(err.retryable !== undefined ? { retryable: err.retryable } : undefined),
+                        ...(err.title !== undefined ? { title: err.title } : undefined),
                     },
                     meta: {
                         command: ctx.path,
@@ -2703,4 +2724,4 @@ function resolveDisplayName(name, aliases) {
         return basename;
     return name;
 }
-//# sourceMappingURL=Cli.js.map
\ No newline at end of file
+//# sourceMappingURL=Cli.js.map
diff --git a/dist/Formatter.js b/dist/Formatter.js
index 4334224..752bd9f 100644
--- a/dist/Formatter.js
+++ b/dist/Formatter.js
@@ -54,15 +54,22 @@ function table(headers, rows) {
     const body = rows.map((r) => `| ${headers.map((_, i) => pad(r[i] ?? '', i)).join(' | ')} |`);
     return `${headerRow}\n${sep}\n${body.join('\n')}`;
 }
+function cell(value) {
+    if (value === undefined)
+        return '';
+    if (isScalar(value))
+        return String(value);
+    return JSON.stringify(value);
+}
 /** Renders a key-value table from a flat object. */
 function kvTable(obj) {
     const entries = Object.entries(obj);
-    return table(['Key', 'Value'], entries.map(([k, v]) => [k, String(v)]));
+    return table(['Key', 'Value'], entries.map(([k, v]) => [k, cell(v)]));
 }
 /** Renders a columnar table from an array of objects. */
 function columnarTable(items) {
     const keys = [...new Set(items.flatMap(Object.keys))];
-    return table(keys, items.map((item) => keys.map((k) => String(item[k] ?? ''))));
+    return table(keys, items.map((item) => keys.map((k) => cell(item[k]))));
 }
 /** Formats a value as Markdown, recursing into nested objects. */
 function formatMarkdown(value, path = []) {
diff --git a/dist/Help.d.ts b/dist/Help.d.ts
index 87bac47..529b6be 100644
--- a/dist/Help.d.ts
+++ b/dist/Help.d.ts
@@ -1,5 +1,10 @@
 import { z } from 'zod';
 import type { GlobalsDescriptor } from './Cli.js';
+/** Registers app-level global flags to include in generated help output. */
+export declare function registerGlobalFlags(flags: ReadonlyArray<{
+    flag: string;
+    desc: string;
+}>): void;
 /** Formats help text for a router CLI or command group. */
 export declare function formatRoot(name: string, options?: formatRoot.Options): string;
 export declare namespace formatRoot {
diff --git a/dist/Help.js b/dist/Help.js
index ad48d9a..c3f120e 100644
--- a/dist/Help.js
+++ b/dist/Help.js
@@ -2,6 +2,11 @@ import { z } from 'zod';
 import { builtinCommands } from './internal/command.js';
 import { toKebab } from './internal/helpers.js';
 import { defaultEnvSource } from './Parser.js';
+const extraGlobalFlags = [];
+/** Registers app-level global flags to include in generated help output. */
+export function registerGlobalFlags(flags) {
+    extraGlobalFlags.push(...flags);
+}
 /** Formats help text for a router CLI or command group. */
 export function formatRoot(name, options = {}) {
     const { aliases, configFlag, description, globals, version, commands = [], root = false, } = options;
@@ -290,6 +295,7 @@ function globalOptionsLines(root = false, configFlag, globals) {
         }
     }
     const flags = [
+        ...extraGlobalFlags,
         ...(configFlag
             ? [{ flag: `--${configFlag} <path>`, desc: 'Load JSON option defaults from a file' }]
             : []),
diff --git a/dist/Mcp.js b/dist/Mcp.js
index d916f16..b0663ff 100644
--- a/dist/Mcp.js
+++ b/dist/Mcp.js
@@ -1,15 +1,13 @@
 import { z } from 'zod';
+import { StdioServerTransport } from '@modelcontextprotocol/server/stdio';
 import * as Command from './internal/command.js';
 import { formatCtaBlock, renderCtaText } from './internal/cta.js';
 import * as Json from './internal/json.js';
 import * as Schema from './Schema.js';
 /** Starts a stdio MCP server that exposes commands as tools. */
 export async function serve(name, version, commands, options = {}) {
-    // Lazy: only runs when actually serving MCP, so plain command runs don't pay for the SDK import.
-    const stdio = importStdioModule();
     const mcp = await import('@modelcontextprotocol/server');
     const { fromJsonSchema, McpServer } = mcp;
-    const StdioServerTransport = await importStdioServerTransport(mcp, stdio);
     const server = new McpServer({ name, ...(options.title ? { title: options.title } : undefined), version }, options.instructions ? { instructions: options.instructions } : undefined);
     registerTools(server, commands, {
         env: options.env,
@@ -26,21 +24,6 @@ export async function serve(name, version, commands, options = {}) {
     const transport = new StdioServerTransport(input, output);
     await server.connect(transport);
 }
-async function importStdioServerTransport(mcp, stdio) {
-    const transport = mcp.StdioServerTransport;
-    if (transport)
-        return transport;
-    const result = await stdio;
-    if (result.error)
-        throw result.error;
-    return result.module.StdioServerTransport;
-}
-function importStdioModule() {
-    return importModule('@modelcontextprotocol/server/stdio')
-        .then((module) => ({ module }))
-        .catch((error) => ({ error }));
-}
-const importModule = (specifier) => import(specifier);
 /** @internal Executes a tool call and returns a CallToolResult. */
 export async function callTool(tool, params, options = {}) {
     const allMiddleware = [
@@ -68,8 +51,36 @@ export async function callTool(tool, params, options = {}) {
         const chunks = [];
         const progressToken = options.extra?.mcpReq?._meta?.progressToken;
         let i = 0;
+        const iterator = result.stream[Symbol.asyncIterator]();
+        let completed = false;
         try {
-            for await (const chunk of result.stream) {
+            while (true) {
+                const item = await iterator.next();
+                if (item.done) {
+                    completed = true;
+                    const returned = item.value;
+                    if (returned !== null &&
+                        typeof returned === 'object' &&
+                        returned[Symbol.for('incur.sentinel')] === 'error') {
+                        const cta = formatCtaBlock(options.name ?? tool.name, returned.cta);
+                        const text = returned.details === undefined
+                            ? (returned.message ?? 'Command failed')
+                            : Json.stringify({
+                                code: returned.code,
+                                details: returned.details,
+                                message: returned.message ?? 'Command failed',
+                                ...(returned.retryable === undefined ? {} : { retryable: returned.retryable }),
+                                ...(returned.title === undefined ? {} : { title: returned.title }),
+                            });
+                        return {
+                            content: [{ type: 'text', text: cta ? `${text}\n\n${renderCtaText(cta)}` : text }],
+                            ...(cta ? { _meta: { cta } } : undefined),
+                            isError: true,
+                        };
+                    }
+                    break;
+                }
+                const chunk = item.value;
                 chunks.push(chunk);
                 if (progressToken !== undefined && options.sendNotification)
                     await options.sendNotification({
@@ -84,11 +95,19 @@ export async function callTool(tool, params, options = {}) {
                 isError: true,
             };
         }
+        finally {
+            if (!completed && iterator.return) {
+                try {
+                    await iterator.return();
+                }
+                catch { }
+            }
+        }
         return { content: [{ type: 'text', text: Json.stringify(chunks) }] };
     }
     if (!result.ok) {
         const cta = formatCtaBlock(options.name ?? tool.name, result.cta);
-        const text = result.error.fieldErrors
+        const text = result.error.fieldErrors || result.error.details !== undefined
             ? JSON.stringify(result.error)
             : (result.error.message ?? 'Command failed');
         return {
@@ -130,6 +149,7 @@ function registerDirectTool(server, tool, options) {
     const hasInput = Object.keys(mergedShape).length > 0;
     server.registerTool(tool.name, {
         ...(tool.description ? { description: tool.description } : undefined),
+        ...(tool.title ? { title: tool.title } : undefined),
         ...(hasInput ? { inputSchema: z.object(mergedShape) } : undefined),
         ...(tool.outputSchema
             ? { outputSchema: options.fromJsonSchema(tool.outputSchema) }
@@ -159,6 +179,7 @@ function registerDiscoveryTools(server, tools, options) {
         return toolResult({
             tools: page.map((tool) => ({
                 name: tool.name,
+                ...(tool.title ? { title: tool.title } : undefined),
                 ...(tool.description ? { description: tool.description } : undefined),
                 ...(tool.annotations ? { annotations: tool.annotations } : undefined),
             })),
@@ -177,6 +198,7 @@ function registerDiscoveryTools(server, tools, options) {
             return toolError(`Unknown tool: ${params.name}`);
         return toolResult({
             name: tool.name,
+            ...(tool.title ? { title: tool.title } : undefined),
             ...(tool.description ? { description: tool.description } : undefined),
             inputSchema: tool.inputSchema,
             ...(tool.outputSchema ? { outputSchema: tool.outputSchema } : undefined),
@@ -284,7 +306,8 @@ function toolError(message) {
 export function collectTools(commands, prefix, parentMiddlewares = [], filter) {
     const tools = filterTools(collectToolEntries(commands, prefix, parentMiddlewares), filter);
     assertUniqueToolNames(tools);
-    return tools.sort((a, b) => a.name.localeCompare(b.name));
+    return tools.sort((a, b) => (a.title ?? a.name).localeCompare(b.title ?? b.name) ||
+        a.name.localeCompare(b.name));
 }
 function collectToolEntries(commands, prefix, parentMiddlewares = []) {
     const result = [];
@@ -306,6 +329,7 @@ function collectToolEntries(commands, prefix, parentMiddlewares = []) {
             const outputSchema = entry.output ? mcpOutputSchema(entry.output) : undefined;
             result.push({
                 name: mcp?.name ?? path.join('_'),
+                ...(mcp?.annotations?.title ? { title: mcp.annotations.title } : undefined),
                 description: mcp?.description ?? entry.description,
                 inputSchema: buildToolSchema(entry.args, entry.options),
                 ...(outputSchema ? { outputSchema } : undefined),
@@ -365,4 +389,4 @@ function buildToolSchema(args, options) {
         return { type: 'object', properties, required };
     return { type: 'object', properties };
 }
-//# sourceMappingURL=Mcp.js.map
\ No newline at end of file
+//# sourceMappingURL=Mcp.js.map
diff --git a/dist/Parser.js b/dist/Parser.js
index f72f537..e27c5d3 100644
--- a/dist/Parser.js
+++ b/dist/Parser.js
@@ -96,8 +96,13 @@ export function parse(argv, options = {}) {
     }
     // Assign positionals to args schema keys in order; a final array key collects the rest
     const rawArgs = {};
+    const argKeys = argsSchema ? Object.keys(argsSchema.shape) : [];
+    const finalKey = argKeys.at(-1);
+    const variadic = argsSchema !== undefined && finalKey !== undefined && isArrayField(finalKey, argsSchema);
+    if (!variadic && positionals.length > argKeys.length)
+        throw new ParseError({ message: `Unexpected argument: ${positionals[argKeys.length]}` });
     if (argsSchema) {
-        const keys = Object.keys(argsSchema.shape);
+        const keys = argKeys;
         for (let j = 0; j < keys.length; j++) {
             const key = keys[j];
             if (isArrayField(key, argsSchema)) {
diff --git a/dist/internal/command.d.ts b/dist/internal/command.d.ts
index 8adad9d..536e25e 100644
--- a/dist/internal/command.d.ts
+++ b/dist/internal/command.d.ts
@@ -60,8 +60,10 @@ export declare namespace execute {
         ok: false;
         error: {
             code: string;
+            details?: unknown;
             message: string;
             retryable?: boolean | undefined;
+            title?: string | undefined;
             fieldErrors?: FieldError[] | undefined;
         };
         cta?: CtaBlock | undefined;
@@ -223,4 +225,4 @@ export declare function findBuiltinSubcommand(builtin: (typeof builtinCommands)[
 }) | undefined;
 /** @internal Checks if a token matches a builtin command by name or alias. */
 export declare function isBuiltin(token: string): boolean;
-//# sourceMappingURL=command.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=command.d.ts.map
diff --git a/dist/internal/command.js b/dist/internal/command.js
index 7011ed7..81121ad 100644
--- a/dist/internal/command.js
+++ b/dist/internal/command.js
@@ -104,8 +104,10 @@ export async function execute(command, options) {
                     ok: false,
                     error: {
                         code: err.code,
+                        ...(err.details !== undefined ? { details: err.details } : undefined),
                         message: err.message,
                         ...(err.retryable !== undefined ? { retryable: err.retryable } : undefined),
+                        ...(err.title !== undefined ? { title: err.title } : undefined),
                     },
                     ...(err.cta ? { cta: err.cta } : undefined),
                     ...(err.exitCode !== undefined ? { exitCode: err.exitCode } : undefined),
@@ -125,8 +127,10 @@ export async function execute(command, options) {
                     ok: false,
                     error: {
                         code: opts.code,
+                        ...(opts.details !== undefined ? { details: opts.details } : undefined),
                         message: opts.message,
                         ...(opts.retryable !== undefined ? { retryable: opts.retryable } : undefined),
+                        ...(opts.title !== undefined ? { title: opts.title } : undefined),
                     },
                     ...(opts.cta ? { cta: opts.cta } : undefined),
                     ...(opts.exitCode !== undefined ? { exitCode: opts.exitCode } : undefined),
@@ -302,4 +306,4 @@ export function findBuiltinSubcommand(builtin, token) {
 export function isBuiltin(token) {
     return builtinCommands.some((b) => b.name === token || b.aliases?.includes(token));
 }
-//# sourceMappingURL=command.js.map
\ No newline at end of file
+//# sourceMappingURL=command.js.map
diff --git a/dist/middleware.d.ts b/dist/middleware.d.ts
index 18a684c..158cbae 100644
--- a/dist/middleware.d.ts
+++ b/dist/middleware.d.ts
@@ -32,9 +32,11 @@ export type Context<vars extends z.ZodObject<any> | undefined = undefined, env e
     error: (options: {
         code: string;
         cta?: CtaBlock | undefined;
+        details?: unknown;
         exitCode?: number | undefined;
         message: string;
         retryable?: boolean | undefined;
+        title?: string | undefined;
     }) => never;
     /** The resolved output format (e.g. `'toon'`, `'json'`, `'jsonl'`). */
     format: Formatter.Format;
@@ -56,4 +58,4 @@ export type Context<vars extends z.ZodObject<any> | undefined = undefined, env e
 /** Creates a strictly typed middleware handler. Pass the vars schema as a generic for typed `c.set()` and `c.var`, the env schema for typed `c.env`, and the globals schema for typed `c.globals`. */
 export default function middleware<const vars extends z.ZodObject<any> | undefined = undefined, const env extends z.ZodObject<any> | undefined = undefined, const globals extends z.ZodObject<any> | undefined = undefined>(handler: Handler<vars, env, globals>): Handler<vars, env, globals>;
 export {};
-//# sourceMappingURL=middleware.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=middleware.d.ts.map
