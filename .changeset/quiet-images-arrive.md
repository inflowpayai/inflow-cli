---
'@inflowpayai/inflow': patch
---

Show Collection and Offering images in their full human-readable details. Show directory search help when no search
input is provided, reject extra positional arguments, distinguish invalid directory responses from request failures,
keep payment options out of the directory's protocol summary, and report missing, malformed, or unsupported AEP claims
with structured remediation details that identify individual address fields.
