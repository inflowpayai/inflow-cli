---
'@inflowpayai/inflow': patch
---

Start the credential vault runtime before every command that can consume vault-backed state, including first-run status
commands and the Windows demand-start service. Normalize top-level command help descriptions, spell out Machine Payments
Protocol, and identify x402 as a protocol.
