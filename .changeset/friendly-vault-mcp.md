---
'@inflowpayai/inflow': patch
---

Keep persistent MCP sessions connected to the local vault, surface streamed command failures as MCP errors, and allow
trusted signed macOS installations to share a compatible vault daemon.
