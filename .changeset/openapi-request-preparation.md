---
'@inflowpayai/inflow': minor
---

Add public OpenAPI request preparation to the CLI and MCP. Select advertised servers, encode supported parameters and
JSON bodies, and preview requests without invoking operations, enrolling, or paying. Redact recognized authentication
headers and declared API-key locations without scanning arbitrary request data.
